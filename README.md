# IMAP-to-SMTP-Send-
I made this to act as a IMAP connector for AliasVault. AliasVault has a SMTP in but is intended to be exposed to the internet as a mail server. I wanted to go this route instead. 
